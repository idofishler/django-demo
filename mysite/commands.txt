# ipython notebook with django
python manage.py shell_plus --notebook

# run server
python manage.py runserver

# create DB schema
python manage.py syncdb

# run tests
python manage.py test

# build as package
python setup.py sdist

# for nicer admin UI
django_admin_bootstrapped
